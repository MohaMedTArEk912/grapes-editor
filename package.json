{
    "name": "grapes-editor",
    "version": "1.0.0",
    "private": true,
    "description": "Grapes IDE - Desktop Visual Full-Stack Builder",
    "scripts": {
        "dev": "cd desktop && npm run tauri dev",
        "build": "cd desktop && npm run tauri build",
        "tauri": "cd desktop && npm run tauri",
        "install:all": "cd desktop && npm install"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2"
    },
    "dependencies": {}
}

