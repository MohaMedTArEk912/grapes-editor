{
    "name": "akasha-ai-chat",
    "version": "1.0.0",
    "description": "Simple AI chat app using OpenRouter + Express",
    "main": "server.js",
    "scripts": {
        "start": "cd server && npm start",
        "dev": "cross-env NODE_OPTIONS=\"--no-deprecation\" concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
        "install:all": "npm install && npm install --prefix server && npm install --prefix client"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.6.1",
        "express": "^4.18.2",
        "openai": "^4.47.1"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "cross-env": "^10.1.0",
        "esbuild": "^0.27.4",
        "nodemon": "^3.1.0"
    }
}
