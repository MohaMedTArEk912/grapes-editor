{
    "name": "akasha",
    "version": "1.0.0",
    "private": true,
    "description": "Akasha — Visually build & export production-ready full-stack SaaS applications",
    "scripts": {
        "dev": "cd desktop && npm run tauri dev",
        "build": "cd desktop && npm run tauri build",
        "tauri": "cd desktop && npm run tauri",
        "install:all": "cd desktop && npm install",
        "dev:log": "set RUST_LOG=info && cd desktop && npm run tauri dev"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2"
    },
    "dependencies": {}
}